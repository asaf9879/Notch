{
  "name": "notch-guardrails-qa",
  "version": "1.0.0",
  "description": "QA test suite for Guardio Automation Audit guardrails feature (config/guardrails)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "@types/node": "^26.4.0",
    "typescript": "^5.5.0"
  }
}
